name: file1
on:
  workflow_dispatch:
    inputs:
      platform:
        description: Platform
        type: String
jobs:
  first_job:
      runs-on: ubuntu-latest
      steps:
          - name: Run first job
            run: |
              echo "The platform is ${{ inputs.platform }}"
